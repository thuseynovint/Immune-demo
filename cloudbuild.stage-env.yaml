#-----------------------------------------
# CloudBuild Pipeline for Staging CloudRun
#-----------------------------------------
steps:
# Docker Build Image
  - name: 'gcr.io/cloud-builders/docker'
    id  : BUILD
    args: ['build', '-t', '$SERVICE_IMAGE', '.', '-f', 'Dockerfile']

substitutions:
    _SERVICE_IMAGE    : northamerica-northeast1-docker.pkg.dev/${PROJECT_ID}/${DOCKER_IMAGENAME}
    _DOCKER_REGISTRY  : immune-test-repo
    _DOCKER_IMAGENAME : immune-test-image
    _PROJECT_ID       : mgmt-348822
    
